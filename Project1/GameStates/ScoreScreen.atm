func(score) {
    print "[ScoreScreen (+)] Press Space To Restart Game!"
    print("Score (" ++ score ++ ")")

    watching :key.dn, :Space {
        every :draw {}
    }

    print "[ScoreScreen (-)]"
}
