require "atmos.env.pico"
require "atmos.env.sdl"

func() {
    print "[TitleScreen (+)] Press Space To Start!"

    watching :key.dn, :Space {
        every :draw {}
    }

    print "[TitleScreen (-)]"
}
